name: sft2d
channels:
  - conda-forge
dependencies:
  - python>=3.9
  # --- core runtime ---
  - numpy>=1.22
  - scipy>=1.8
  - astropy>=5.0
  - pandas>=1.4
  - matplotlib>=3.5
  # --- tests + notebooks ---
  - pytest>=7.0
  - jupyter
  - ipykernel
  # Install the package itself (editable) into the environment:
  - pip
  - pip:
      - -e .
